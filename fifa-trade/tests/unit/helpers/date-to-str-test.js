import { dateToStr } from 'fifa-trade/helpers/date-to-str';
import { module, test } from 'qunit';

module('Unit | Helper | date to str');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateToStr([42]);
  assert.ok(result);
});
